package cl.uchile.dcc.citric
package model.abstractclasses

import model.traits.Units

abstract class AbstractFighter extends Units{

  /** private var _Hp: Int = maxHp */


  /** Setter of the Hp of the fighter.
   *
   * This function might be invoked in a battle.
   *
   *  NewHp The new Hit Points of the fighter.
   */

    /**
  def Hp_=(NewHp: Int): Unit = {
    if (NewHp <= maxHp && NewHp >= 0) {
      _Hp = NewHp
    }
  } */



}
